import 'package:aeris/src/models/pipeline.dart';

/// Call to interact with Aeris' Back end
class AerisAPI {
  Future<void> createPipeline(Pipeline newPipeline) {
  /// Send Pipeline to API
    return Future.delayed(Duration.zero);
  }
}
